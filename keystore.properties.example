# Copy this file to `keystore.properties` and fill real values.
# The file `keystore.properties` is ignored by git.

storeFile=keystore/finance-release.jks
storePassword=CHANGE_ME
keyAlias=finance_release
keyPassword=CHANGE_ME
